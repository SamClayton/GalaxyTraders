# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.2.0               # Packages every Meteor app needs to have
shell-server@0.3.0              # Server-side component of the `meteor shell` command.
mobile-experience@1.0.5         # Packages for a great mobile UX
mongo@1.3.1                  # The database Meteor supports right now
blaze-html-templates@1.0.4      # Compile .html files into Meteor Blaze views
reactive-var@1.0.11             # Reactive variable for tracker
tracker@1.1.3                   # Meteor's client-side reactive programming library
# jquery@1.11.9                 # jQuery client-side library
# random                        # PRNG

standard-minifier-css@1.3.5     # CSS minifier run for production mode
standard-minifier-js@2.2.0      # JS minifier run for production mode
es5-shim@4.6.15                 # ECMAScript 5 compatibility for older browsers.
ecmascript@0.9.0                # Enable ECMAScript2015+ syntax in app code

# autopublish@1.0.7               # Publish all data to the clients (for prototyping)
# insecure@1.0.7                  # Allow all DB writes from clients (for prototyping)

twbs:bootstrap                  # Bootstrap CSS framework
kadira:flow-router              # Routing
kadira:blaze-layout             # Layout

useraccounts:core               # Needed for AccountsTemplates.configure ?
accounts-base@1.4.0                   # Base user accounts package
accounts-password@1.5.0         # username/password login to account
accounts-facebook@1.3.0        # Facebook user login
# accounts-github               # Github user login
# accounts-google               # Google user login
ian:accounts-ui-bootstrap-3     # UI for user accounts including Facebook and username/password logins
alanning:roles                  # Roles for users, including ACLs (?)
utilities:avatar                # Support for user's profile photo
# useraccounts:flow-routing     # UserAccounts package providing routes configuration capability via kadira:flow-router
# useraccounts:bootstrap        # Accounts Templates styled for Twitter Bootstrap

browser-policy@1.1.0            # CSP package. Careful, because it's already caused some CSP headaches with web fonts
underscorestring:underscore.string      # String manipulation extensions for Underscore.js javascript library (used for number string formatting)
3stack:bignumber                # Dealing with large numbers. AAT deals with these often

aldeed:simple-schema            # Used in the LevelUpTut Intermediate Meteor, also by Collections2 and AutoForm used there
aldeed:schema-index             # Control Mongo indexes through SimpleSchema
aldeed:collection2              # Added because the Meteor Guide covers how to auto-enforce Simple Schema on mutators (CUD ops)
aldeed:autoform@5.0.0           # Works with collection2 and schemas to autogenerate forms
momentjs:moment                 # Dependency of autoform, but also useful for general Date math

juliancwirko:s-alert            # Simple and fancy notifications / alerts / errors for Meteor
juliancwirko:s-alert-jelly      # Jelly visual effect for s-alert

meteortoys:allthings            # Dev tools yay "Since Meteor Toys are a debugOnly package, Meteor's build process will not compile it into production code."
# alanning:trace                  # Client console trace output for devs
# okland:pidusage                 # System monitoring including cpu usage


# packages to consider
# dburles:collection-helpers
# matb33:collection-hooks
# ongoworks:security            # Recommended by autoform's author to sanitize and enforce security on collections
# aldeed:schema-deny
# juliancwirko:s-alert          # Simple and fancy notifications / alerts / errors for Meteor
# chrismbeckett:toastr          # Gnome / Growl type non-blocking notifications
# mouse0270:bootstrap-notify    # Turns standard Bootstrap alerts into "Growl-like" notifications
# practicalmeteor:munit         # Meteor unit testing framework for packages
# tinytest                      # Tinytest is the official Meteor package test runner
# mike:mocha                    # Run mocha tests in the browser
# velocity:core                 # Velocity, a Meteor specific test-runner
# fortawesome:fontawesome       # Font Awesome (official): 500+ scalable vector icons, customizable via CSS, Retina friendly
# session                       # Session is a special ReactiveDict whose contents are preserved across Hot Code Push
# arillo:flow-router-helpers    # Template helpers for flow-router

# This one may be particularly good for replacing "scheduler"
# vsivsi:job-collection          # A persistent and reactive job queue for Meteor, supporting distributed workers that can run anywhere
facebook-config-ui
dynamic-import
